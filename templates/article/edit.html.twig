{% extends 'base.html.twig' %}

{% block title %}{{ 'article.edit_title'|trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-pencil"></i> {{ 'article.edit_title'|trans }} : {{ article.designation }}</h4>
                </div>
                <div class="card-body">
                    {{ include('article/_form.html.twig', {'button_label': 'article.save_modifications'|trans}) }}
                </div>
            </div>

            <div class="mt-3">
                <a href="{{ path('app_article_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> {{ 'common.back_to_list'|trans }}
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
