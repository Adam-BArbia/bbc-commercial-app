{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.users_new'|trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="h3 mb-3"><i class="bi bi-person-plus"></i> {{ 'admin.create_user'|trans }}</h1>
    <div class="card">
        <div class="card-body">
            {% include 'admin/user/_form.html.twig' with {form: form, button_label: 'common.save'|trans} %}
        </div>
    </div>
</div>
{% endblock %}
