{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.edit_user'|trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="h3 mb-3"><i class="bi bi-pencil-square"></i> {{ 'admin.edit_user'|trans }} {{ userEntity.name }}</h1>
    <div class="card">
        <div class="card-body">
            {% include 'admin/user/_form.html.twig' with {form: form, button_label: 'common.update'|trans} %}
        </div>
    </div>
</div>
{% endblock %}
