{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.roles_new'|trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="h3 mb-3"><i class="bi bi-plus-square"></i> {{ 'admin.create_role'|trans }}</h1>
    <div class="card">
        <div class="card-body">
            {% include 'admin/role/_form.html.twig' with {form: form, button_label: 'common.save'|trans} %}
        </div>
    </div>
</div>
{% endblock %}
